costbase = 10;
const altura = 5;
const area = base * altura;
const perímetro = (base * 2) + (altura * 2);

console.log(`Área do retangulo: ${area}`);
console.log(`perímetro do retangulo: ${perímetro}`);
